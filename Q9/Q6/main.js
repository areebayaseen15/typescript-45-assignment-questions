"use strict";
/*Qno6: Stripping Names: Store a person’s name, and include some whitespace characters at the beginning
and end of the name. Make sure you use each character combination, "\t" and "\n", at least once.
 Print the name once, so the whitespace around the name is displayed.
 Then print the name after striping the white spaces.*/
let name = "\t\n Areeba Quraishi \t\n"; // thid saves the name with whitespace
console.log(name); //shows the name with whitespace
console.log(name.trim()); //shows the name without space
